RSpec.describe StarwarsCharacters do
  it "has a version number" do
    expect(StarwarsCharacters::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
